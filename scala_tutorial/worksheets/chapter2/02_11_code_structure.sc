/*
2.11 파일과 이름공간으로 코드 구조화하기

자바와 다르게 파일 이름은 타입 이름과 일치할 필요가 없고, 패키지 구조는 디렉터리 구조와 일치할 필요가 없다.
src/main/scala/package_example.scala

연속 패키지 문 관용구. 해당 패키지를 가져와서 추가/코딩할 수도 있다.
src/main/scala/package_example3.scala

 */


